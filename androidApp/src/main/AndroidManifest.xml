<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- OBLIGATORIO para Ktor: llamadas a la API de Groq -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Los audios se guardan temporalmente en la caché interna de la app;
         no se requiere permiso de almacenamiento. -->

    <!-- Necesario desde Android 13 para poder mostrar notificaciones en la barra del sistema -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- El sensor es opcional para conservar compatibilidad con emuladores. -->
    <uses-feature
        android:name="android.hardware.sensor.proximity"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:exported="true"
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>